{
  "name": "totaluptime-reskin",
  "version": "0.1.0",
  "private": true,
  "description": "Total Uptime — Edgenexus reskin prototype. Static HTML + Tailwind CSS.",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./dist/tailwind.css --minify",
    "dev":   "tailwindcss -i ./src/input.css -o ./dist/tailwind.css --watch",
    "serve": "python3 -m http.server 8000",
    "start": "npm run dev & npm run serve"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
